/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.pinterest.com', 'example.com', 'images.unsplash.com', 'lh3.googleusercontent.com', 's.gravatar.com'], // Agrega todos los dominios necesarios
    },
};

export default nextConfig;
