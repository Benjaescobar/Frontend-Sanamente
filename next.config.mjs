/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.pinterest.com', 'example.com', 'images.unsplash.com', 'lh3.googleusercontent.com', 'files.edgestore.dev'], // Agrega todos los dominios necesarios
    },
};

export default nextConfig;
